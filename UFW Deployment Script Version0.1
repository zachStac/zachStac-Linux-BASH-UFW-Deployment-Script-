#BASH UFW firewall deployment script

#install UFW
apt update
apt upgrade 
sleep 1
apt-get isntall ufw 
sleep 1
ufw --version 

#update ufw
apt update
apt upgrade 
sleep 1
apt-get update ufw
sleep 1
ufw --version


#enable ufw
enable ufw

#disable ufw
disable ufw

#set aplication rules

#ip whitelisting 


#set port rules

#ufw default 
sudo ufw default allow outgoing
sudo ufw default deny incoming

#enable IPV6

#disable IPV6

#enable IPV4

#disable IPV6

#portsd neeeded are absed on the demands of the machine or the comp
#building a list of ports for basic use and security use, like most of the demands the script will ned to built seprate. 
#if the server is an applicatoin server there will most likly be some kind of scoket conections, these ports will need to be defined if the ips are static thne they shu be added to some kind of an ACL list


